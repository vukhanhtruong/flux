{
  "mcpServers": {
    "flux": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "flux_mcp.server"],
      "env": {
        "DATABASE_PATH": "${DATABASE_PATH}"
      }
    }
  }
}
